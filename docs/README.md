# docs

Holds `demo.gif`, referenced from the top-level README.

See "Recording the demo" in the root README for the recipe. Keep it under ~5MB — GitHub will
serve larger files, but they stall on slow connections, and the demo is the first thing anyone
looks at.
